[behave]
# Write Allure results on every run (view with: allure serve allure-results)
# and keep the human-readable pretty output in the console
format = pretty
    allure_behave.formatter:AllureFormatter
outfiles = -
    allure-results
